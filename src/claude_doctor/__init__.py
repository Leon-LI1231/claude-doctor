"""claude-doctor — Claude Code 体检医生。

一个纯本地 CLI 工具，扫描 `~/.claude` 配置健康度。
设计参考 omc-replica：venv + .cmd wrapper + click CLI。
"""
from __future__ import annotations

__version__ = "0.1.0"
__all__ = ["__version__"]